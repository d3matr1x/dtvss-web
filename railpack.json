{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": [
        "echo '=== DIAGNOSTIC: working directory ===' && pwd",
        "echo '=== DIAGNOSTIC: full file listing ===' && ls -la",
        "echo '=== DIAGNOSTIC: requirements files visible to build ===' && ls -la requirements* 2>&1 || echo 'no requirements* match'",
        "echo '=== DIAGNOSTIC: end ==='",
        "python -m pip install --upgrade pip",
        "pip install --require-hashes -r requirements-lock.txt"
      ]
    }
  }
}
