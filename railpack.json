{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "python": "3.12"
  },
  "steps": {
    "install": {
      "commands": [
        "python -m pip install --upgrade pip",
        "echo '=== about to run pip install --require-hashes ===' && echo 'python:' && python --version && echo 'pip:' && pip --version && echo 'pwd:' && pwd && echo 'lockfile head:' && head -5 requirements-lock.txt && echo '=== pip install output: ===' && pip install --require-hashes -r requirements-lock.txt"
      ]
    }
  }
}
